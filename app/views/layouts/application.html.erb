<!DOCTYPE html>
<html>
  <head>
    <title>C12 Branches Library</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <nav>
      <ul>
        <li>
          <%# This is the syntax for defining a link in HTML (More in depth in View Helpers lesson) %>
          <%= link_to "Home!", root_path %>
        </li>
        <li>
          <%= link_to "Go to the Books Index Page", books_path %>
        </li>
                <li>
          <%= link_to "Go to the Authors Index Page", authors_path %>
        </li>
        <li>
          <%= link_to "Let's make a NEW book", new_book_path %>
        </li>
        <li>
          <%= link_to "Log In!", login_path %>
        </li>
        <li>
          <%= link_to "Current User", current_user_path %>
        </li>
        <li>
          <%= link_to "Log Out!", logout_path, method: :post %>
        </li>
      </ul>
    </nav>

    <%# Our nice generic way of displaying flash messages: Iterating over the hash-like structure of flash and setting all HTML elements and CSS classes however we want. %>
    <section class="user-messages">
      <% flash.each do |name, message| %>
        <div class="<%= name %>"><%= name.capitalize %>: <%= message %></div>
      <% end %>
    </section>


    <%= yield %>
  </body>
</html>
